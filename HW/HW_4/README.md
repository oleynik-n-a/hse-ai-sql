# HW 4. Оконные функции

14 обязательных задач и 5 бонусных. Обязательная часть – **20 баллов**: Q01–Q08 по 1 баллу, Q09–Q14 по 2 балла. Q15–Q19 – бонус по 1 баллу, максимум **25**. Нерешённый бонус обязательные 20 не снижает.

Сдавайте один файл с именем по корпоративной почте:

- `avivanov@edu.hse.ru` → `avivanov_hw4.sql`;
- `avivanov_1@edu.hse.ru` → `avivanov_1_hw4.sql`.

Сложность растёт по материалу пары:

1. Q01–Q04 – окно, партиция, условие;
2. Q05–Q07 – рамка;
3. Q08–Q10 – место в порядке;
4. Q11–Q14 – соседние значения и `bookings`;
5. Q15–Q19 – бонус: группы, доли, `GROUPS`, `EXCLUDE`, `NTH_VALUE`.

**Что проверяет чекер:** во всех задачах, включая бонус, нужен `OVER`. Остальное – по результату.

В каждой задаче выведите **только перечисленные колонки, в этом порядке**. Сортировка – часть ответа. Если не написано `DESC`, сортируйте по возрастанию. Псевдонимы таблиц и форматирование можно выбирать самостоятельно.

---



### Q01, 1 балл

Таблица `course.daily_airport_metrics`. Для каждой её строки выведите `airport_code`, `report_date`, `flight_count` и сумму `flight_count` по **всем** строкам таблицы как `total_flights`.

Это сумма значений `flight_count`, не число строк. Одна строка результата – один аэропорт в одну дату. У всех строк `total_flights` одинаковый. Отсортируйте по `airport_code`, затем по `report_date`.

### Q02, 1 балл

Таблица `course.daily_airport_metrics`. Для каждой её строки выведите `airport_code`, `report_date`, `flight_count` и сумму `flight_count` по строкам **этого аэропорта** как `airport_flights`.

Это снова сумма `flight_count`, не число строк. Отсортируйте по `airport_code`, затем по `report_date`.

### Q03, 1 балл

Таблица `course.daily_airport_metrics`. Для каждой её строки выведите `airport_code`, `report_date`, `flight_count`, `revenue` и сумму `flight_count` по дням **этого аэропорта**, у которых `revenue >= 12000`, как `flights_on_high_revenue_days`.

Строки с `revenue < 12000` оставьте в результате, в сумму их `flight_count` не включайте. Порог `12000` входит в сумму. Суммируйте `flight_count`, не `revenue`. Отсортируйте по `airport_code`, затем по `report_date`.

### Q04, 1 балл

Таблица `course.daily_airport_metrics`. Разложите **все** дни таблицы на две партиции: строки с `flight_count >= 6` и остальные. Порог `6` входит в первую партицию.

Для каждой строки выведите `airport_code`, `report_date`, `flight_count`, `revenue` и сумму `revenue` внутри этой партиции как `size_revenue`.

По аэропорту не режьте: дни разных аэропортов с `flight_count >= 6` лежат в одной партиции. Колонку с названием партиции не выводите. Отсортируйте по `airport_code`, затем по `report_date`.

### Q05, 1 балл

Таблица `course.daily_airport_metrics`. Для каждого аэропорта посчитайте накопительную сумму `flight_count` по возрастанию `report_date`: от самой ранней даты аэропорта до текущей строки включительно.

Считайте физические строки, не группы одинаковых значений. Выведите `airport_code`, `report_date`, `flight_count`, накопительную сумму как `running_flights`. Отсортируйте по `airport_code`, затем по `report_date`.

### Q06, 1 балл

Таблица `course.daily_airport_metrics`. Для каждого аэропорта посчитайте среднее `flight_count` по текущей строке и двум предыдущим **строкам** этого аэропорта по возрастанию `report_date`.

Это соседние строки по дате, не «минус два календарных дня». Если предыдущих строк меньше двух, считайте среднее по тем, что есть. Выведите `airport_code`, `report_date`, `flight_count`, среднее как `moving_avg_flights`. Округлите **готовое** среднее до двух знаков, не каждый `flight_count` по отдельности. Отсортируйте по `airport_code`, затем по `report_date`.

### Q07, 1 балл

Таблица `course.daily_airport_metrics`. Для каждого аэропорта посчитайте накопительную сумму `revenue` по возрастанию `revenue`.

Внутри окна сортируйте **только** по `revenue`, без `report_date`. Дни с одинаковой выручкой в одном аэропорте входят в сумму вместе: у них одно и то же `running_revenue_by_value`.

Выведите `airport_code`, `report_date`, `revenue`, сумму как `running_revenue_by_value`. Итог отсортируйте по `airport_code`, затем по `revenue`, затем по `report_date`.

### Q08, 1 балл

Таблица `course.daily_airport_metrics`. Внутри каждого аэропорта пронумеруйте строки уникальными номерами `1`, `2`, `3`, … по возрастанию `report_date`.

Выведите `airport_code`, `report_date`, `flight_count`, номер как `day_number`. Итог отсортируйте по `airport_code`, затем по `day_number`.

### Q09, 2 балла

Таблица `course.daily_airport_metrics`. Внутри каждого аэропорта сравните места дней по убыванию `revenue`.

Выведите `airport_code`, `report_date`, `revenue`, место с пропусками как `revenue_rank` и место без пропусков как `revenue_dense_rank`.

В оконной сортировке используйте **только** `revenue DESC`, без даты. При равной выручке оба места одинаковые. Итог отсортируйте по `airport_code`, затем по `revenue DESC`, затем по `report_date`.

### Q10, 2 балла

Таблица `course.daily_airport_metrics`. Для каждого аэропорта оставьте ровно два дня.

Сначала внутри аэропорта выдайте уникальный номер: по `revenue DESC`, при равенстве выручки – по возрастанию `report_date` (раньше дата – меньше номер). Затем оставьте строки с номером `1` и `2`.

Выведите только `airport_code`, `report_date`, `revenue`. Сам номер не выводите. Отсортируйте по `airport_code`, затем по `revenue DESC`, затем по `report_date`.

### Q11, 2 балла

Таблица `course.daily_airport_metrics`. Для каждого аэропорта возьмите выручку предыдущей строки по возрастанию `report_date`.

Выведите `airport_code`, `report_date`, `revenue`, предыдущее значение как `previous_revenue` и разницу `revenue - previous_revenue` как `revenue_change`.

У самой ранней даты аэропорта предыдущей строки нет: `previous_revenue` и `revenue_change` равны `NULL`. Не заменяйте их на `0`. Отсортируйте по `airport_code`, затем по `report_date`.

### Q12, 2 балла

Таблица `timetable`. Оставьте рейсы с `flight_id <= 20`, граница входит.

Выведите `flight_id`, `departure_airport`, `scheduled_departure` и время следующего вылета из того же аэропорта как `next_departure`.

Следующий – по возрастанию `scheduled_departure`, при равенстве времени – по возрастанию `flight_id`. Если следующего рейса нет, `next_departure` равен `NULL`. Итог отсортируйте по `departure_airport`, затем по `scheduled_departure`, затем по `flight_id`.

### Q13, 2 балла

Таблица `course.daily_airport_metrics`. Для каждой строки выведите выручку самой ранней и самой поздней **даты** этого аэропорта.

Это не минимум и не максимум `revenue`. Нужны колонки `airport_code`, `report_date`, `revenue`, `first_revenue`, `last_revenue`. Внутри аэропорта идите по возрастанию `report_date`. Рамка – от первой до последней строки аэропорта, чтобы у всех дней аэропорта стояла одна и та же пара. Отсортируйте по `airport_code`, затем по `report_date`.

### Q14, 2 балла

Соедините `segments` и `tickets` по `ticket_no`. Оставьте бронь `book_ref = '0053GE'`.

Сначала отберите эти сегменты, затем считайте окна только по ним. Выведите `ticket_no`, `flight_id`, `fare_conditions`, `price`, среднее `price` данного класса обслуживания среди сегментов этой брони как `class_avg_price` и сумму `price` всех сегментов данного билета как `ticket_total`.

Одна строка результата – один сегмент этой брони. Округлите **готовое** среднее до двух знаков. Отсортируйте по `ticket_no`, затем по `flight_id`.

---



### Q15, бонус, 1 балл

Таблица `course.daily_airport_metrics`. Разрежьте все её строки на три группы близкого **числа строк** функцией `NTILE(3)` по убыванию `revenue`. Это не порог в рублях.

Выведите `airport_code`, `report_date`, `revenue`, номер группы как `revenue_group`. Группа `1` – начало очереди (самая большая выручка). При равной выручке порядок в окне: `airport_code`, затем `report_date`. Итог отсортируйте по `revenue_group`, затем по `revenue DESC`, затем по `airport_code`, затем по `report_date`.

### Q16, бонус, 1 балл

Таблица `course.daily_airport_metrics`. Для каждой строки посчитайте `PERCENT_RANK` и `CUME_DIST` по возрастанию `revenue` по **всей** таблице, без партиции по аэропорту.

В оконной сортировке используйте **только** `revenue`, без даты и без аэропорта. При равной выручке значения одинаковые.

Выведите `airport_code`, `report_date`, `revenue`, `percent_rank`, `cume_dist`. Оба значения приведите к `numeric` и округлите до двух знаков. Отсортируйте по `revenue`, затем по `airport_code`, затем по `report_date`.

### Q17, бонус, 1 балл

Таблица `course.daily_airport_metrics`. Для каждого аэропорта выстройте дни по возрастанию `revenue`. Внутри окна сортируйте **только** по `revenue`.

Посчитайте сумму `flight_count` по текущей группе одинаковой выручки и **одной предыдущей** такой группе. Считайте соседние группы равных значений, не физические строки и не «плюс-минус один рубль».

Выведите `airport_code`, `report_date`, `revenue`, `flight_count`, сумму как `nearby_group_flights`. У дней с одинаковой выручкой в одном аэропорте значение одинаковое. Итог отсортируйте по `airport_code`, затем по `revenue`, затем по `report_date`.

### Q18, бонус, 1 балл

Таблица `course.daily_airport_metrics`. Для каждой строки посчитайте сумму `revenue` дней **того же аэропорта**, у которых `revenue` **другое**, чем у текущей строки.

Дни с той же выручкой, включая текущую строку, в сумму не входят. Рамка – все дни аэропорта. Выведите `airport_code`, `report_date`, `revenue`, сумму как `unlike_revenue`. Отсортируйте по `airport_code`, затем по `report_date`.

### Q19, бонус, 1 балл

Таблица `course.daily_airport_metrics`. Для каждого аэропорта возьмите выручку **второй** строки, если дни аэропорта упорядочить по `revenue DESC`, при равенстве – по возрастанию `report_date`.

Рамка – от первой до последней строки аэропорта: одно и то же число должно стоять у всех дней аэропорта. Это вторая строка после сортировки, не «место 2». Выведите `airport_code`, `report_date`, `revenue`, это значение как `second_highest_revenue`. Отсортируйте по `airport_code`, затем по `report_date`.